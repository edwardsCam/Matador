;
/*
function blankPattern(prms) {
    return {
        params: prms || {}, // programmer-defined values that can be used to tweak behavior of patterns.
        init: function() { console.log('Blank init function!'); }, // one-time function for building necessary objects.
        destroy: function() { console.log('Blank destroy function!'); }, // one-time function for reallocating memory and resetting any variables that may be used by another pattern.
        draw: function() {} // called every frame. responsible for altering model, not view.
    };
}
*/
